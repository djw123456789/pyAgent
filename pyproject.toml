[project]
name = "pyagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "danjiawen", email = "12484215+danjiawen@user.noreply.gitee.com" }
]
requires-python = ">=3.13"
dependencies = [
    "aiosqlite>=0.22.1",
    "alembic>=1.19.1",
    "bcrypt>=5.0.0",
    "email-validator>=2.3.0",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "pydantic-settings>=2.15.0",
    "python-jose[cryptography]>=3.5.0",
    "redis>=5.0",
    "sqlmodel>=0.0.39",
    "uvicorn[standard]>=0.52.4",
]

# [project.scripts]
# pyagent = "pyagent:main"
# [build-system]
# requires = ["uv_build>=0.12.5,<0.13.0"]
# build-backend = "uv_build"
